

params {

    samplesheet = ''
    downsample = false
    build_seurat = false
}

profiles {
    standard {
        process.executor = 'local'
    }

    singularity {
        singularity.enabled = true
        docker.enabled = false
    }

    docker {
        docker.enabled = true
        singularity.enabled = false
    }
}
